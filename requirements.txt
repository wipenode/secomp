# Runtime dependencies (see pyproject.toml for authoritative version ranges)
boto3>=1.34,<2.0
click>=8.1,<9.0
rich>=13.0,<15.0
pydantic>=2.5.0,<3.0.0

# Test dependencies
pytest>=7.4
moto[s3]>=5.0
pytest-mock>=3.12

# Optional cloud provider SDKs
# Install with: pip install .[azure] or pip install .[gcp]
azure-storage-blob>=12.19
azure-identity>=1.15
google-cloud-storage>=2.14
